{
  "name": "card-transactions-publisher",
  "version": "1.0.0",
  "description": "publishes random raw card transactions on specified interval in .env file",
  "main": "src/index.js",
  "author": "Andrew Roberts",
  "license": "MIT",
  "scripts": {
    "build": "babel src -d dist --presets @babel/preset-env",
    "dev": "nodemon --watch src --exec babel-node src/index.js",
    "start": "nodemon --exec babel-node src/index.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.3",
    "@babel/core": "^7.8.3",
    "@babel/node": "^7.8.4",
    "@babel/preset-env": "^7.8.3",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "core-js": "^3.6.4",
    "dotenv": "^8.2.0",
    "immer": "^9.0.6",
    "kafka-node": "^5.0.0",
    "mqtt": "^3.0.0",
    "regenerator-runtime": "^0.13.3"
  }
}
